{
  "name": "aromamatrix-website",
  "version": "1.0.0",
  "private": true,
  "description": "Multilingual B2B fragrance manufacturing website built with Eleventy and Nunjucks",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "eleventy --serve",
    "clean": "node scripts/clean-docs.js",
    "build": "npm run clean && eleventy",
    "test": "node --test"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  }
}
